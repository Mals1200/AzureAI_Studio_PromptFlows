name: Deploy to Azure Web App

on:
  push:
    branches:
      - Streamlit-consume  # Adjust to your branch name if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12'  # Adjust the Python version if necessary

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r "Streamlit app/requirements.txt"  # Ensure this path is correct

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: cxqa-streamlit-webapp
        slot-name: production
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISH_PROFILE }}  # Ensure this secret is set
